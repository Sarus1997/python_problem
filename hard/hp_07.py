
# โจทย์: แก้ไขโค้ดต่อไปนี้ให้ถูกต้อง

"""
    from functools import lru_cache

    class MyClass:
        @lru_cache(maxsize=10)
        def compute(self, x):
            return x * x

    obj = MyClass()
    print(obj.compute(4))  # TypeError: ทำไมถึงเกิดปัญหา?

"""

# คำตอบ:
